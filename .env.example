PORT= # e.g. 3000
# Database
DATABASE_URL=postgresql://wassup:changeme@localhost:5432/wassup
POSTGRES_PASSWORD=changeme  # used by Docker Compose for the postgres service

GITHUB_TOKEN= # for Github widget

# NextAuth.js — generate with: openssl rand -base64 32
AUTH_SECRET=
AUTH_TRUST_HOST=true   # required when running in Docker or behind a proxy
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
