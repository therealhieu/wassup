services:
  wassup:
    build: .
    # image: ghcr.io/therealhieu/wassup:latest  # use when pulling from registry
    ports:
      - "3000:3000"
    volumes:
      - wassup-data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  wassup-data:
